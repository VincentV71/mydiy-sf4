<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <meta content="width = device-width, initial-scale = 1, shrink-to-fit = no" name="viewport">
        <title>MyDiy est une application qui vous aide à gérer vos recettes de e-liquide : dosage "Do-It-Yourself" (arôme, base, temps de steep), archivage de recettes avec notation, commentaires, etc...</title>
        <link crossorigin="anonymous" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" rel="stylesheet">
        <link href="static/css/style.css" rel="stylesheet">
    </head>
    <body>
        {# NAVBAR #}
        <nav
            class="navbar navbar-toggleable-sm navbar-light navbar-inverse">
            {# Container pour limiter la largeur LOGO + Item Menu #}
            <div
                class="container" role="navigation">
                {# = container FIXED #}
                <a class="navbar-brand justify-content-start hidden-sm-down" href="{{ path('home') }}">
                    <img height="80" id="#logoNav" role="navigation" src="static/img/logo-noir.png" width="80">
                </a>
                {# Toggler qui se déplie et qui prend diyNav#}
                <button
                    aria-controls="diyNav" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler navbar-toggler mt-3" data-target="#diyNav" data-toggle="collapse" type="button">
                    {# icon type Burger #}
                    <span class="navbar-toggler-icon"></span>
                </button>
                {# Source du Toggler : #}
                <div class="collapse navbar-collapse justify-content-end" id="diyNav">
                    <div class="navbar-nav text-uppercase ">
                        <a class="nav-item nav-link" href="{{ path('home') }}">Accueil</a>
                        <a class="nav-item nav-link" href="{{ path('newdiy') }}">Nouveau DIY</a>
                        <a class="nav-item nav-link" href="{{ path('mydiy') }}">Mes recettes</a>
                        <a class="nav-item nav-link" href="{{ path('register') }}">S'inscrire</a>
                        {% if not app.user %}
                            <a class="nav-item nav-link" href="{{ path('login') }}">Se connecter</a>
                        {% else %}
                            <a class="nav-item nav-link" href="{{ path('logout') }}">Deconnexion</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        {# Affiche les MessageFlash Symfony (stockés dans session) #}
        {% for label, messages in app.flashes(['warning', 'error', 'info']) %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        {% block body %}{% endblock %}

        <script crossorigin="anonymous" integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU=" src="https://code.jquery.com/jquery-3.4.1.js"></script>
        <script crossorigin="anonymous" src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.3.7/js/tether.js"></script>
        <script crossorigin="anonymous" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>

        {#  <script src="../public/static/js/jquery-3.1.1.slim.min.js"  type="text/javascript"></script> #}
    {#  <script src="../public/static/js/tether.min.js"  type="text/javascript"></script> #}
        {#  <script src="../public/static/js/bootstrap.min.js"  type="text/javascript"></script> #}
    </body>
</html>
