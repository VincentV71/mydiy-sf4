<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta content="width = device-width, initial-scale = 1, shrink-to-fit = no" name="viewport">
    <title>
        {% block title %}
        MyDiy est une application qui vous aide à gérer vos recettes de e-liquide : dosage "Do-It-Yourself" (arôme,
        base, temps de steep), archivage de recettes avec notation, commentaires, etc...
        {% endblock %}
    </title>
    <link href="https://fonts.googleapis.com/css?family=Saira+Condensed:800" rel="stylesheet">
    <link href="{{ asset('static/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('static/css/style.css') }}" rel="stylesheet">
    {% block js_head %}{% endblock js_head %}
</head>

<body>
    {# NAVBAR #}
    <nav class="navbar navbar-toggleable-sm navbar expand-md navbar-light navbar-inverse">
        <div class="container" role="navigation">
            <a class="navbar-brand justify-content-start hidden-sm-down" href="{{ path('home') }}">
                <img height="80" id="#logoNav" role="navigation" src="{{ asset('static/img/logo-noir.png') }}"
                    width="80">
            </a>
            <button aria-controls="diyNav" aria-expanded="false" aria-label="Toggle navigation"
                class="navbar-toggler navbar-toggler mt-3" data-target="#diyNav" data-toggle="collapse" type="button">
                <span class="navbar-toggler-icon"></span>
            </button>
            <a data-toggle="collapse" href="#diyNav"></a>

            <div class="collapse navbar-collapse justify-content-end" id="diyNav">
                <div class="navbar-nav text-uppercase ">
                    <a class="nav-item nav-link" href="{{ path('home') }}">Accueil</a>
                    <a class="nav-item nav-link" href="{{ path('newdiy') }}">Nouveau DIY</a>
                    <a class="nav-item nav-link" href="{{ path('mydiy') }}">Mes recettes</a>
                    <a class="nav-item nav-link" href="{{ path('register') }}">S'inscrire</a>
                    {% if not app.user %}
                    <a class="nav-item nav-link" href="{{ path('login') }}">Se connecter</a>
                    {% else %}
                    <a class="nav-item nav-link" href="{{ path('logout') }}">Deconnexion</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    {# Affiche les MessageFlash Symfony (stockés dans session) #}
    <div class="">
        {% for label, messages in app.flashes(['warning', 'error', 'info', 'success']) %}
        {% for message in messages %}
        <div class="alert alert-{{ label }} pl-4 mb-0">
            <div class="container">
                {{ message }}
            </div>
        </div>
        {% endfor %}
        {% endfor %}
    </div>

    {% block body %}{% endblock %}

    <script src="{{ asset('static/js/jquery-3.1.1.slim.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('static/js/tether.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('static/js/popper.js') }}" type="text/javascript"></script>
    <script src="{{ asset('static/js/bootstrap.min.js') }}" type="text/javascript"></script>
</body>

</html>